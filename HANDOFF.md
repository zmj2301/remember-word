# 记单词工具 —— 项目交接文档

> 最后更新：2026-07-30
> 状态：**MVP 基本功能完成，有两个待改进项**

---

## 一、项目概述

这是一个**纯前端网页应用**，专为**初中生背英语单词**设计，目标运行在**科大讯飞 AI 学习机 T20**（安卓平板，自带浏览器，家长管控白名单上网）。

### 核心卖点
- **双模式**：「记单词」（纯滑动浏览）+「默写」（学习+拼写检验）
- **刷视频式交互**：上下滑动切换卡片
- **艾宾浩斯曲线复习**：1/2/4/7/15 天间隔，5 次复习即掌握
- **多维助记**：每张卡片含 emoji图标、音标、释义、词根、联想、例句
- **零成本**：纯前端，无服务器、无 API，数据存浏览器 localStorage

---

## 二、产品决策记录（与用户讨论确认的）

| 决策项 | 结论 |
|--------|------|
| 产品形态 | 纯前端 Web 网页（讯飞 T20 浏览器打开，家长加白名单） |
| 核心定位 | 刷视频式交互 + 艾宾浩斯曲线，不含 AI 功能 |
| 记忆内容 | 语境/阅读驱动 + 词根联想 + 例句 + emoji 图片 + 默写 |
| 目标用户 | 初中词汇（当前内置 30 词演示） |
| 默写方式 | 看中文释义，打英文拼写 |
| 每日节奏 | 每天新词（默认 10 个）+ 到期复习，先复习后学新 |
| 数据存储 | 当前：localStorage 本地存储。长期：阿里云 ECS 后端 + 手机端 App + 家长端进度面板 |
| 浏览模式 | 仅浏览今日队列（到期复习+新词），纯滑动不记录进度 |

---

## 三、技术架构

### 技术选型
- **原生 HTML/CSS/JS**，零框架、零依赖、不联网
- 存储：`localStorage`（JSON）
- 未来可加 PWA 支持离线

### 文件结构
```
记单词工具/
├── index.html          # 单页应用入口（5 个屏幕：首页/学习/完成/统计/设置）
├── css/
│   └── style.css       # 全部样式，含卡片动画、平板适配、双模式按钮
├── js/
│   ├── app.js          # 主逻辑（含内联的艾宾浩斯 SRS 算法 + 双模式路由）
│   ├── dict.js         # 词库（30 个初中词，每词 9 字段）
│   ├── store.js        # localStorage 存储模块（进度/设置/导出/导入）
│   └── swipe.js        # 上下滑动手势检测（触屏+鼠标兼容）
├── data/
│   └── README.md       # Excel 导入词库的格式规范
└── README.md           # 项目说明 + 讯飞 T20 使用指引
```

### 数据格式

**单词数据**（js/dict.js 中每个词条）：
```json
{
  "id": "apple",
  "word": "apple",
  "phonetic": "/ˈæpl/",
  "pos": "n.",
  "meaning": "苹果",
  "emoji": "🍎",
  "root": "基础词，无明显词根。",
  "mnemonic": "联想：苹果公司 Apple，手机就叫它",
  "example": "I eat an apple every day.",
  "exampleTrans": "我每天吃一个苹果。"
}
```

**学习进度**（localStorage key: `vocab_progress_v1`）：
```json
{
  "apple": {
    "stage": 0,
    "dueDate": "2026-07-31",
    "learnedDate": "2026-07-30",
    "history": [{"date":"2026-07-30","result":"correct"}]
  }
}
```
- stage: 0=新词，1~4=复习中，5=已掌握（5 次全对）
- dueDate: 下次复习日期，null 表示已掌握不再复习

### SRS 算法（内联在 app.js）
- 间隔表：`[1, 2, 4, 7, 15]` 天
- 答对 → stage+1，用对应间隔设下次 dueDate
- 答错 → stage 退一格（最低 0），dueDate 设为明天
- stage 达到 5 → 已掌握，不再进入复习队列
- 已通过 **28 项自动化断言验证**（Node 模拟测试）

### 页面状态机
```
home（首页概览）
  ├─ 点击「记单词」→ study（浏览模式，纯滑动）
  └─ 点击「默写」  → study（默写模式：学习面→默写面→结果面）
study 完成后 → done（完成页）→ 返回 home
底部导航：首页 / 统计 / 设置
```

---

## 四、当前完成状态

### ✅ 已完成
| 功能 | 状态 |
|------|------|
| 30 个初中词汇库（含 emoji/音标/词根/联想/例句） | ✅ |
| 艾宾浩斯间隔重复算法（28 项测试通过） | ✅ |
| localStorage 存储读写/导出/导入/重置 | ✅ |
| 双模式入口：「记单词」纯浏览 +「默写」学习+检验 | ✅ |
| 卡片 6 类信息渲染（emoji/音标/释义/词根/联想/例句） | ✅ |
| 默写交互（看中文打英文，对错判断，进度更新） | ✅ |
| 今日队列计算（到期复习优先，新词按上限截断） | ✅ |
| 统计页（已学/已掌握/正确率/进度条） | ✅ |
| 设置页（每日新词数/导出导入/清空进度） | ✅ |
| 响应式样式（平板适配，600px+ 大屏增强） | ✅ |
| 触屏+鼠标上下滑动手势 | ✅ |
| 语法检查全部通过 | ✅ |
| Node 模拟加载全部模块验证通过 | ✅ |

### ⚠️ 已知待改进（用户明确提出）

#### 1. 滑动动画要真实连贯
**用户原话**：「我想要的是一个真实的滑动效果，而不是只是一个切面。是要有一个从旧的那一页逐渐退出去，新的一页逐渐上来的这种感觉，而且要连贯一点。」

**当前问题**：卡片切换是简单的显示/隐藏（CSS `display:none/active`），没有过渡动画。

**改进方案建议**：
- 用 CSS `transform: translateY()` + `transition` 做平滑上移
- 或者用两张卡片容器：当前卡片上滑出去、新卡片从底部滑入，形成类似抖音/TikTok 的效果
- 考虑 `touchmove` 事件做跟手拖拽（手指拖到哪里卡片就跟到哪里）
- `swipe.js` 目前只检测滑动阈值，需要扩展为跟手拖拽 + 惯性/回弹

#### 2. 浏览器端到端交互验证未完成
**原因**：ZCode 的浏览器自动化工具（IAB）在中途丢失连接，无法恢复。Playwright 的 `getByRole` 对含 emoji 子元素的按钮点击频繁超时。

**需要做的**：手动在浏览器打开 `http://localhost:8000/`（先运行 `python -m http.server 8000`），验证：
- 首页双按钮是否正常显示
- 点「记单词」进入浏览模式，滑动是否流畅，不进默写
- 点「默写」进入默写模式，完整走一遍学习→默写→结果
- 关闭浏览器重开，进度是否保留

---

## 五、后续路线图（与用户讨论确认的）

| 阶段 | 内容 | 技术要点 |
|------|------|---------|
| **当前** | 修复滑动动画 + 端到端验证 | CSS transition/animation + touchmove 跟手 |
| 第 2 阶段 | 阿里云 ECS 后端 + 进度上云 + 手机端同址访问 | Node 后端 + MySQL + 认证机制 + 家长白名单放行域名 |
| 第 3 阶段 | 家长端进度面板（学习天数/正确率/待复习数） | 后端 API + 家长手机浏览器访问 |
| 词库扩充 | ECDICT 开源词库打底 + 用户 Excel 导入 | Excel→JSON 转换脚本，`data/README.md` 已有格式规范 |

### 用户资源
- 有阿里云 ECS 服务器（经济型）
- 会提供初中词汇 Excel 文件（按 `data/README.md` 格式）
- 目标设备：讯飞 T20 学习机 + 手机

---

## 六、如何继续开发

### 本地启动
```bash
cd "E:\coding-zhou\AI 编程\记单词工具"
python -m http.server 8000
# 浏览器访问 http://localhost:8000
```

### 关键设计决策（不要再改的）
1. **纯前端零依赖**：讯飞 T20 兼容性优先，不引框架
2. **SRS 内联在 app.js**：之前 srs.js 跨文件全局暴露在浏览器中失败（原因未完全查明），内联后问题消除。不要拆出去
3. **不用 AI**：用户明确要求零成本无 AI，助记内容由词库预置
4. **emoji 代替图片**：学习机联网受限，不用外部图片源

### 调试注意
- IAB（ZCode 内置浏览器）对含 emoji 的按钮点击不稳定，手动测试更可靠
- Playwright evaluate 是只读的，不能用来调试 DOM 写入
- `node --check` 只查语法，不查运行时；用 Node eval 模拟浏览器全局可验证大部分逻辑

---

## 七、市调参考（选读）

项目初期对市面上主流记单词工具做了调研，核心结论：

| 工具 | 核心打法 | 启示 |
|------|---------|------|
| Anki | 最优 SRS 算法（SM-2/FSRS） | 间隔重复是科学基础 |
| 墨墨背单词 | 个性化抗遗忘+数据可视化 | 算法+可视化增强信心 |
| 不背单词 | 真实语境浸入式学习 | 语境例句比死记更持久 |
| 百词斩 | 图片联想+趣味 | 但"会选≠真记住" |
| 2025 趋势 | AI 个性化助记、短视频式碎片化 | 但本项目用户要求零成本无 AI |

**记住单词的底层逻辑**：编码（多通道助记）× 巩固（SRS 间隔重复），两者缺一不可。
